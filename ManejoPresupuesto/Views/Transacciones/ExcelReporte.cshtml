@{
    ViewData["Title"] = "Reporte en Excel";
    var hoy=  DateTime.Today;
    var haceUnMes = hoy.AddMonths(-1);
    var mesActual = hoy.Month;
    var añoActual = hoy.Year;
    var mesAnterior = haceUnMes.Month;
    var mesAnteriorAño = haceUnMes.Year;
    var añoAnterior = hoy.AddYears(-1);

}

<partial name="_Submenu" model="SubMenuTransacciones.Excel" />

<h1>Exportar Transacciones al Excel</h1>

<div class="mb-3">
    <a asp-action="ExportarExcelPorMes" class="btn btn-success" asp-route-mes="@mesActual" asp-route-año="@añoActual">
        Exportar Mes Actual
    </a> ~ @hoy.ToString("MMMM yyyy")
</div>

<div class="mb-3">
    <a asp-action="ExportarExcelPorMes" class="btn btn-success" asp-route-mes="@mesAnterior" asp-route-año="@mesAnterior">
        Exportar Mes Anterior
    </a> ~ @haceUnMes.ToString("MMMM yyyy")
</div>

<div class="mb-3">
    <a asp-action="ExportarExcelPorAño" class="btn btn-success" asp-route-año="@añoActual">
        Exportar Año Actual
    </a> ~ @añoActual
</div>

<div class="mb-3">
    <a asp-action="ExportarExcelPorAño" class="btn btn-success" asp-route-año="@añoAnterior">
        Exportar Año Anterior
    </a> ~ @añoAnterior
</div>

<div class="mb-3">
    <a asp-action="ExportarExcelTodo" class="btn btn-success" asp-route-año="@añoActual">
        Exportar Todo
    </a> ~ Todos
</div>
